specVersion: "v1beta"
serviceId: "wonka"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wonka-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
